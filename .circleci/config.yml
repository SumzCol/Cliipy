version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.9.5-node
    steps:
      - checkout
      - run: cliipy
      - sonarcloud/scan
orbs:
  sonarcloud-python: ksmc/sonarcloud-python@0.0.1
workflows:
  main:
    jobs:
      - build:
          context: SonarCloud