name: Greet first-time contributors

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Wow! this your fist issue on Cliipa!!! Congratulations¡!¡!'
        pr-message: 'This is the first PR of this user in the Cliipa Repo. Congratulate they¡!'
